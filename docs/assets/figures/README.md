# Figuras LaTeX (pacote académico externo)

As figuras finais em PDF (`cap1-fluxo-logico.pdf`, `cap4-ingestao-massa.pdf`, etc.) e as fontes
(Mermaid, TikZ, PlantUML) vivem no pacote académico externo, em `assets/figures/`.

Este site operacional publica apenas os diagramas PlantUML/Mermaid na secção [Diagrama mestre — fluxo lógico](../diagrams/diagrama-mestre.svg).
