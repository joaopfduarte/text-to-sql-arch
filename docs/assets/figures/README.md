# Figuras LaTeX (repositório da monografia)

As figuras finais em PDF (`cap1-fluxo-logico.pdf`, `cap4-ingestao-massa.pdf`, etc.) e as fontes
(Mermaid, TikZ, PlantUML) vivem no repositório da monografia, em `assets/figures/`.

Este site operacional publica apenas os diagramas PlantUML/Mermaid em [`../diagrams/`](../diagrams/).
